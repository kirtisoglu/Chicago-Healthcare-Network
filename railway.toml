[build]
builder = "nixpacks"

[deploy]
startCommand = "panel serve healthcare/dashboard.py --address 0.0.0.0 --port $PORT --allow-websocket-origin='*' --num-procs 1"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
